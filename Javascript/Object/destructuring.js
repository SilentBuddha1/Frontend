// Destructuring in obj

let user = {
    name: "Ramchandra",
    age: 24,
    address: "Dolakha",
}

const {name} = user;
const {age, address} = user;

console.log(name);
console.log(age);
console.log(address);